import torch
import torch.nn as nn

class ELA(nn.Module):
    # Stub added because original repo authors forgot to commit this file
    def __init__(self, c1=0, c2=0, *args, **kwargs):
        super().__init__()
        
    def forward(self, x):
        return x
